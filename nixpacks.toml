providers = ['rust', 'node','...']

[phases.setup]
nixPkgs = ['openssl','pkg-config','...']
nixOverlays = [
    'https://github.com/dreadsfun/rust-overlay/archive/master.tar.gz',
]

[phases.install]
cmds = ["..."]

[phases.build]
cmds = ["..."]