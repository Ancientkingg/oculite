providers = ['rust', 'node','...']

[phases.setup]
nixLibs = ['openssl', '...']
nixOverlays = [
    'https://github.com/dreadsfun/rust-overlay/archive/master.tar.gz',
]

[phases.install]
cmds = ["..."]

[phases.build]
cmds = ["..."]